function derivar=derivar (x)
% function derivar=derivar (x)
% Función que deriva una función introducida como parámetro.
% PARAMETROS DE ENTRADA:
%               x:  señal a derivar.
% PARAMETROS DE SALIDA:
%               deriva: señal de entrada derivada.

derivar=diff(x);
